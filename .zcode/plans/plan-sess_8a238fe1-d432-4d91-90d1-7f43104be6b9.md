# AI分析目录下新增「大盘分析」「板块分析」菜单

## 已确认的决策（AskUserQuestion 未获答复，按推荐项执行）
1. **挂载位置**：与「AI 分析」平级，挂在「AI助手」目录（sys_menu id `2942406616008001`）下，sort=4/5，不动现有页面
2. **页面形态**：数据摘要（指数/资金流/板块排行）+ AI 生成的大盘点评 / 板块轮动解读
3. **AI 实现**：落库型异步分析——新建 `analysis` 模块，模式对齐策略 run（submit_run 落库即返、后台 LLM、三态 status、历史记录、收盘后定时生成）
4. **板块成分股**：本期不做，板块分析基于现有板块日快照

## 一、数据库（alembic 0021 + 模型）

**新迁移** `backend/alembic/versions/0021_add_market_sector_analysis.py`（`down_revision='0020'`，照抄 0015 的 `_MENU_TABLE/_menu_row/_btn_row` helper）：

1. **新表 `business_analysis_run`**（AI 分析执行记录）：
   - Base 列（id 雪花/deleted_at/created_at/updated_at）
   - `analysis_type` String(20) — market/sector；`run_date` String(10)；`trigger_type` String(20) — schedule/manual
   - `status` String(20) 三态 running/success/failed（对齐 0020 后的策略 run）
   - `ai_raw_response` Text（markdown 报告原文）、`parsed_result` JSON（结构化摘要，解析失败为 NULL）、`error_msg` Text
   - 索引：id unique、(analysis_type, created_at)、(analysis_type, run_date)
2. **sys_menu 种子**（8012–8015，parent=8001）：
   - `2942406616008012`：name=`ai_market-analysis`，path=`/ai/market-analysis`，component=`view.ai_market-analysis`，permission=`analysis:market:list`，icon=`mdi:chart-arespline`→用`mdi:chart-areaspline`，MENU，sort=4
   - `2942406616008013`：name=`ai_sector-analysis`，path=`/ai/sector-analysis`，component=`view.ai_sector-analysis`，permission=`analysis:sector:list`，icon=`mdi:view-grid-outline`，MENU，sort=5
   - BUTTON：8014（parent=8012，`analysis:run`）、8015（parent=8013，`analysis:run`）——「生成分析」按钮权限
   - **关键约束**：sys_menu.name 必须与前端 elegant-router 生成的路由 name 逐字一致（0018 踩坑记录）
   - 按仓库惯例不写 sys_role_menu（超管免授权可见；其他角色上线后角色管理页勾选）
   - downgrade：DELETE sys_menu WHERE id IN (8012..8015) + drop 表
3. **模型** `backend/database/models/business/analysis.py` 新建 `BusinessAnalysisRun`（写法对齐 strategy.py，MappedAsDataclass + insert_default），在 `business/__init__.py` 导出

## 二、后端 analysis 模块（`backend/modules/analysis/`，含 endpoints/services/schemas/__init__.py）

**AnalysisExecutor**（模式照抄 `strategy_executor.py`）：
- `submit_run(db, analysis_type, trigger_type) -> run_id`：同 type 已有 running → 抛新错误码 `ANALYSIS_ALREADY_RUNNING`（加到 `core/response/response_code.py`，对齐 STRATEGY_ALREADY_RUNNING 写法）；创建 running 记录 commit 后 `asyncio.create_task` 后台执行（_BACKGROUND_TASKS 强引用）
- `_execute_analysis`：独立 session（`get_session`）+ `asyncio.wait_for` 600s 兜底 + 异常回写 failed/error_msg
- `_analyze`：
  1. **直接注入数据**（不走 ReAct 工具循环，更快可控）：market 用 `MarketService.get_indices + get_fund_flow` + 涨停情绪 stats；sector 用 `BoardService.get_list`（industry/concept 各 top N，含主力净流入/领涨股/涨跌家数）
  2. LLM：复用 `modules/agent/services/llm_client.resolve_model`（function 取 `AiFunctionEnum.TREND_PREDICTION`，LLM配置页可绑专属模型）+ `stream_chat` 聚合完整文本；系统提示词要求先输出 JSON 摘要（market：sentiment/score/summary/key_points；sector：hot_boards[]/rotation_summary/key_points）再输出 markdown 报告
  3. 解析：照抄 `_extract_json_array` 思路提取 ```json 块，失败则仅存原文；回写 success + `ai_raw_response[:20000]` + parsed_result
- 同日去重：schedule 触发当日已有 success 则跳过；manual 不受限

**endpoints**（前缀对齐现有注册方式，`backend/main.py` include_router；统一 `{code,msg,data,request_id,err_code}` 响应，失败也走 `success(data=result)` 防泛型 500 坑）：
- `POST /admin/analysis/{analysis_type}/run`（权限 analysis:run）→ 返回 run_id
- `GET /admin/analysis/{analysis_type}/latest` → 最新一条（含原文）
- `GET /admin/analysis/runs` → 分页历史（query：analysis_type/page/page_size；**Enum query 字段用 Annotated+BeforeValidator，空/非法返 None 防 422**）
- `GET /admin/analysis/runs/{run_id}` → 详情
- schemas：AnalysisType、run 列表/详情响应模型（enum 序列化对齐现有写法）

## 三、调度任务 `backend/modules/scheduler/tasks/analysis_run.py`

- `analysis_auto_generate`：cron `5 16 * * mon-fri`（在 market_sync 15:30 / board_sync 15:31 / limit_up 15:35 之后），task_key=`analysis.auto_generate`
- 依次对 market/sector 检查当日 success 去重后 submit_run(trigger_type='schedule')，捕获 CustomError（running 守卫）
- 星期字段必须 `mon-fri`（APScheduler Monday=0 坑，任务注释已有记录）

## 四、前端

1. **类型/API**：`frontend/src/typings/api/analysis.d.ts`（AnalysisRun/Detail/分页 query，对齐 strategy.d.ts 风格）+ `frontend/src/service/api/analysis.ts`（run/latest/runs/detail 四个函数）；指数/资金流/板块排行直接复用现有 stock API
2. **页面**：
   - `frontend/src/views/ai/market-analysis/index.vue`（name: AiMarketAnalysis）：顶部 7 大指数卡片（涨红跌绿规则对齐 a-stock/market-overview）+ 资金流摘要；中部 AI 大盘点评——「生成分析」按钮（hasPermission analysis:run）→ POST run → 轮询状态（running 加载态）→ markdown-it 渲染报告（复用 ai/chat 的 MarkdownIt 用法）+ parsed_result 情绪/评分徽章；历史记录分页列表（抽屉或表格，点击看详情）
   - `frontend/src/views/ai/sector-analysis/index.vue`（name: AiSectorAnalysis）：顶部板块排行表（industry/concept Tab 切换，参考 a-stock/industry-board）；AI 板块轮动解读同上模式（hot_boards 高亮）；历史记录同上
   - 两页共用的「报告卡片+生成按钮+历史列表」逻辑优先抽到 `frontend/src/components/`（实现时按项目组件目录惯例定位，无惯例则各页内实现）
3. **i18n**：zh-cn.ts route 块加 `'ai_market-analysis': '大盘分析'`、`'ai_sector-analysis': '板块分析'`；en-us.ts 对应 'Market Analysis'/'Sector Analysis'；page 块按现有 page.ai.* 组织补页面文案；同步 `frontend/src/typings/app.d.ts` 的 App.I18n.Schema
4. **路由生成**：`pnpm gen-route` 重新生成 elegant-router 四件套（routes/imports/transform/elegant-router.d.ts），确认生成 `ai_market-analysis`/`ai_sector-analysis` 与 sys_menu.name 一致

## 五、文档与记忆（AGENTS.md 要求）
- 新增 `aiDoc/memory/business/2026-08-19_market_sector_analysis_menu.md` + 更新 `aiDoc/memory/business/README.md` 需求索引
- `aiDoc/frontend-backend/boundary.md` 补 analysis 模块 4 个接口契约

## 六、验证
1. `alembic upgrade head`（0021）
2. 后端语法/启动验证；超管登录确认新菜单出现、`POST /admin/analysis/market/run` 手动触发生成并轮询到 success
3. 前端 `pnpm gen-route` → `pnpm typecheck`

## 注意事项
- 0020 迁移当前为未提交状态，0021 以其为 down_revision，与工作区现状一致
- 不做：板块成分股表/同步、sys_role_menu 授权写入、北向资金等新数据源